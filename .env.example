# Public site origin, used for canonical URLs + sitemap. No trailing slash.
VITE_SITE_URL=https://www.rexertech.com

# Optional. POST endpoint for the contact form (Formspree, Basin, your own API, ...).
# If unset, the form validates and shows success without sending anything.
VITE_CONTACT_ENDPOINT=
